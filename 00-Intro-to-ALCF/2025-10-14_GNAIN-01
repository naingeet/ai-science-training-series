Homework 1

#Make sure that the single GPU code runs on Polaris- 
-Yes! its working, example python script is inside folder 
https://github.com/naingeet/ai-science-training-series/tree/main/00-Intro-to-ALCF/GNAIN-01/run_ddp_pytorch_2p8_N1_R1.sh with update NRANKS and NRANKS_PERNODE parameters
and it takes total train time: 19.04s with 1 GPU

#The counting of ranks, does not necessarily has to be a mix-and-match between mpi4py and PALS. Try to implement the rank counting method using just PALS or mpi4py. device_count() methods can be useful here.
-done implementaion of rank counting using mpi4py with below funtion
from mpi4py import MPI
def get_local_rank():
    comm = MPI.COMM_WORLD
    local_comm = comm.Split_type(MPI.COMM_TYPE_SHARED)
    return local_comm.Get_rank()
LOCAL_RANK = get_local_rank()

Local Rank with 1 GPU and 1 NODE is 0

#Play with different dimensions of the src and tgt tensors.
-larger dimensions takes longer time.

#Explore the cost of collective communication, by setting up a scenario, where you have only two ranks, but each rank resides on a different node. 
#Profile and try to reason about the results.
-It takes longer in communcating with different nodes vs when both ranks (GPU) were used from same node. 

#Try other file formats to explore the I/O bottleneck.
-numpy is okay with 1 GPU but does not seem suitable for distributed training compared to hdf5

#Make the tensors really large, specially the 2nd and 3rd dimension and explore different data types.
-exploring!
